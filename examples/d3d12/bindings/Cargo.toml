[package]
name = "d3d12_bindings"
version = "0.9.0"
authors = ["Microsoft"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
windows = { path = "../../.." }

[build-dependencies]
windows = { path = "../../.." }
