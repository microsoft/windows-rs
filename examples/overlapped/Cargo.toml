[package]
name = "overlapped"
version = "0.0.0"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
bindings = { package = "overlapped_bindings", path = "bindings" }
windows = { path = "../.." }
