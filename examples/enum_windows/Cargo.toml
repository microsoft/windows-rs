[package]
name = "enum_windows"
version = "0.5.0"
authors = ["Kenny Kerr <kenny.kerr@microsoft.com>"]
edition = "2018"

[dependencies]
bindings = { package = "enum_windows_bindings", path = "bindings" }
windows = { path = "../.." }
