[package]
name = "enum_windows_bindings"
version = "0.9.1"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../../.." }

[build-dependencies]
windows = { path = "../../.." }
