[package]
name = "spellchecker"
version = "0.0.0"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
bindings = { package = "spellchecker_bindings", path = "bindings" }
windows = { path = "../.." }