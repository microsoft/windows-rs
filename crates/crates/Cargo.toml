[package]
name = "windows_crates"
version = "0.7.0"
authors = ["Microsoft"]
edition = "2018"
license = "MIT OR Apache-2.0"
description = "Code generation for the windows crate"

[dependencies]
gen = { package = "windows_gen", path = "../gen", version = "0.7.0" }
