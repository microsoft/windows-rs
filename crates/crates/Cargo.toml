[package]
name = "windows_crates"
version = "0.9.0"
authors = ["Microsoft"]
edition = "2018"
license = "MIT OR Apache-2.0"
description = "Generates crates for the Windows APIs"

[dependencies]
gen = { package = "windows_gen", path = "../gen", version = "0.9.0" }
