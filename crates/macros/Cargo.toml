[package]
name = "winrt-macros"
version = "0.8.0"
authors = ["Microsoft"]
edition = "2018"
license = "MIT"
description = "Macros for the winrt crate"

[lib]
proc-macro = true

[dependencies]
syn = "1.0"
quote = "1.0"
squote = "0.1.2"
proc-macro2 = "1.0"
winrt-gen = { path = "../gen", version = "0.8.0" }
winmd = { path = "../winmd",  version = "0.8.0" }
rayon = "1.3"
