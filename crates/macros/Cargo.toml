[package]
name = "winrt_macros"
version = "0.7.1"
authors = ["Microsoft"]
edition = "2018"
license = "MIT"
description = "Macros for the winrt crate"

[lib]
proc-macro = true

[dependencies]
syn = "1.0"
quote = "1.0"
proc-macro2 = "1.0"
winrt_gen = { path = "../gen", version = "0.7.1" }
