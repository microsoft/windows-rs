[package]
name = "windows_gen"
version = "0.21.1"
authors = ["Microsoft"]
edition = "2018"
license = "MIT OR Apache-2.0"
description = "Code gen support for the windows crate"

[dependencies]
quote = { package = "windows_quote", path = "../quote", version = "0.21.1" }
reader = { package = "windows_reader", path = "../reader", version = "0.21.1" }
