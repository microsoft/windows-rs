[package]
name = "windows_gen"
version = "0.18.0"
authors = ["Microsoft"]
edition = "2018"
license = "MIT OR Apache-2.0"
description = "Code gen support for the windows crate"

[dependencies]
quote = { package = "windows_quote", path = "../quote", version = "0.18.0" }
winmd_reader = { package = "windows_winmd_reader", path = "../winmd_reader", version = "0.18.0" }


[features]
raw_dylib = []
