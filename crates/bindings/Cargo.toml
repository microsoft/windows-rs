[package]
name = "windows_bindings"
version = "0.0.0"
edition = "2018"
publish = false

[dependencies]
reader = { package = "windows_reader", path = "../reader", version = "0.22.0" }
macros = { package = "windows_macros", path = "../macros", version = "0.22.0" }
