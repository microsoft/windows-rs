[package]
name = "windows_bindings"
version = "0.0.0"
edition = "2018"

[dependencies]
windows_macros = { path = "../macros" }
reader = { package = "windows_reader", path = "../reader", version = "0.18.0" }
