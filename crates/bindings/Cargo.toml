[package]
name = "windows_bindings"
version = "0.0.0"
edition = "2018"

[dependencies]
windows_macros = { path = "../macros" }
winmd_reader = { package = "windows_winmd_reader", path = "../winmd_reader", version = "0.18.0" }
