[package]
name = "reactor_swap_chain_panel"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
reactor_samples = { path = "../samples" }
windows-canvas.workspace = true
windows-core.workspace = true
windows-reactor = { workspace = true, features = ["canvas"] }

[build-dependencies]
windows-reactor-setup = { workspace = true }

[lints]
workspace = true
