[package]
name = "reactor-webview"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
windows-reactor = { workspace = true }
windows-webview = { workspace = true, features = ["reactor", "system"] }

[build-dependencies]
windows-reactor-setup = { workspace = true }

[lints]
workspace = true
