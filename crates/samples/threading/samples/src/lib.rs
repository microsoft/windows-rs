//! Samples for the `windows-threading` crate.
//!
//! Each file in `examples/` is a standalone program demonstrating the Windows
//! thread pool. Run one with `cargo run -p threading_samples --example <name>`.
