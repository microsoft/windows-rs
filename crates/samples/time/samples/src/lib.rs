//! Samples for the `windows-time` crate.
//!
//! Each file in `examples/` is a standalone program demonstrating the `TimeSpan`
//! and `DateTime` value types. Run one with
//! `cargo run -p time_samples --example <name>`.
