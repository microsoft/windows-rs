//! Raw `windows-sys` samples.
//!
//! Each file in `examples/` is a standalone program that calls the Windows API
//! directly through the zero-overhead `windows-sys` bindings. Run one with
//! `cargo run -p windows_sys_samples --example <name>`.
