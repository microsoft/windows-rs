//! Samples for the `windows-registry` crate.
//!
//! Each file in `examples/` is a standalone program that reads and writes the
//! Windows registry under `HKEY_CURRENT_USER` and cleans up after itself. Run
//! one with `cargo run -p registry_samples --example <name>`.
