[package]
name = "composition-canvas"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
windows-canvas = { workspace = true, features = ["composition", "system"] }
windows-composition = { workspace = true, features = ["system"] }
windows-core = { workspace = true }
windows-window = { workspace = true }

[lints]
workspace = true
