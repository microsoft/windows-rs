//! Samples for the `windows-strings` crate.
//!
//! Each file in `examples/` is a standalone program demonstrating the Windows
//! string types. Run one with `cargo run -p strings_samples --example <name>`.
