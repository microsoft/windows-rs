[package]
name = "test_winui_bench_host"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
windows = { workspace = true, features = ["winuser"] }
windows-core = { workspace = true }
windows-reactor = { workspace = true }

[lints]
workspace = true
