//! Samples for the `windows-future` crate.
//!
//! Each file in `examples/` is a standalone program demonstrating the stock
//! WinRT async types. Run one with
//! `cargo run -p future_samples --example <name>`.
