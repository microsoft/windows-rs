[package]
name = "windows-memory-buffer"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
windows = { workspace = true, default-features = true, features = [
    "Foundation",
    "memorybuffer",
] }

[lints]
workspace = true
