[package]
name = "windows-privileges"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
windows = { workspace = true, default-features = true, features = [
    "handleapi",
    "securitybaseapi",
    "winbase",
    "winerror",
    "winnt",
] }

[lints]
workspace = true
