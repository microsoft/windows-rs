[package]
name = "windows-window-message"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
windows = { workspace = true, default-features = true, features = ["winuser"] }
windows-window = { workspace = true }

[lints]
workspace = true
