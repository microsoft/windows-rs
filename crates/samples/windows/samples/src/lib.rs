//! Samples for the `windows` crate.
//!
//! Each file in `examples/` is a standalone program demonstrating one area of
//! the Windows API — Win32, COM, or WinRT — projected by the `windows` crate.
//! Run one with `cargo run -p windows_samples --example <name>`.
