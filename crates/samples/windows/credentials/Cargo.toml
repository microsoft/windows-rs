[package]
name = "windows-credentials"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
windows = { workspace = true, default-features = true, features = ["wincred"] }

[lints]
workspace = true
