[package]
name = "windows-thread-pool-work"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
windows = { workspace = true, default-features = true, features = ["threadpoolapiset"] }

[lints]
workspace = true
