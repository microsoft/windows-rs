[package]
name = "sample_enum_windows"
version = "0.0.0"
edition = "2021"

[dependencies.windows]
path = "../../libs/windows"
features = [
    "Win32_Foundation",
    "Win32_UI_WindowsAndMessaging",
]
