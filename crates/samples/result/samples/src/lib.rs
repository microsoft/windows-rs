//! Samples for the `windows-result` crate.
//!
//! Each file in `examples/` is a standalone program demonstrating Windows error
//! handling and propagation. Run one with
//! `cargo run -p result_samples --example <name>`.
