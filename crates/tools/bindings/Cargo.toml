[package]
name = "windows_bindings"
version = "0.0.0"
edition = "2018"
publish = false

[dependencies]
reader = { package = "windows_reader", path = "../../deps/reader", version = "0.28.0" }
bindgen = { package = "windows-bindgen", path = "../../deps/bindgen", version = "0.28.0" }
