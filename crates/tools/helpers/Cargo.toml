[package]
name = "helpers"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
windows-link = { workspace = true }
toml = { version = "0.8", features = ["parse"] }
serde = { version = "1.0", features = ["derive"] }
