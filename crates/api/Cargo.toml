[package]
name = "windows_api"
version = "0.22.0"
edition = "2018"
publish = false

[dependencies]
reader = { package = "windows_reader", path = "../reader", version = "0.22.0" }
gen = { package = "windows_gen", path = "../gen", version = "0.22.0" }
rayon = "1.5.1"
