[package]
name = "windows-riddle"
version = "0.0.0"
edition = "2021"
rust-version = "1.74"
license = "MIT OR Apache-2.0"
description = "Parser-generator for Windows metadata"
repository = "https://github.com/microsoft/windows-rs"
readme = "readme.md"

[dependencies]
windows-ecma335 = { workspace = true }
syn = { workspace = true, features = ["full", "parsing"] }
