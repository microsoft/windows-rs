[package]
name = "test_error"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
doc = false
doctest = false

[dependencies.windows]
workspace = true
features = [
    "bcrypt",
    "Foundation",
    "ntstatus",
    "rpc",
    "std",
    "winerror",
]

[dev-dependencies]
helpers = { workspace = true }

[lints]
workspace = true
