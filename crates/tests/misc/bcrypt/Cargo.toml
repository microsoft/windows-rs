[package]
name = "test_bcrypt"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
doc = false
doctest = false

[dependencies.windows]
workspace = true
features = [
    "bcrypt",
    "ntstatus",
]

[dependencies.windows-sys]
workspace = true
features = [
    "bcrypt",
    "ntstatus",
]

[lints]
workspace = true
