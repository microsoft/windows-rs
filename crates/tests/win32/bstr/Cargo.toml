[package]
name = "test_win32_bstr"
version = "0.0.0"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../../../.." }

[build-dependencies]
windows = { path = "../../../..", features = ["build"] }

[features]
default = ["std"]
std = []
