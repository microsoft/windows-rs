[package]
name = "test_winrt_collections"
version = "0.0.0"
authors = ["Microsoft"]
edition = "2018"

[dependencies.windows]
path = "../../../.."
features = [
    "std",
    "alloc",
    "Foundation_Collections",
]

[build-dependencies]
windows = { path = "../../../..", features = ["build"] }
