[package]
name = "test_winrt_method_names"
version = "0.0.0"
authors = ["Microsoft"]
edition = "2018"

[dependencies.windows]
path = "../../../libs/windows"
features = [
    "Foundation",
]

[build-dependencies]
windows = { path = "../../../libs/windows", features = ["build"] }
