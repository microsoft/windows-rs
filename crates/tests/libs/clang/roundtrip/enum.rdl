#[win32]
mod Test {
    #[repr(u32)]
    enum FooEnum {
        A = 0,
        B = 1,
        C = 2,
    }
    #[library("test.dll")]
    extern fn FooFunction(foo: FooEnum);
    #[repr(u32)]
    enum Name {
        One = 1,
        Two = 2,
    }
    const ONE: u32 = 1;
    const TWO: u32 = 2;
}
