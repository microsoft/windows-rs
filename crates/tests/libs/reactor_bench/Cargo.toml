[package]
name = "test_reactor_bench"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
test_reactor = { path = "../reactor" }
windows-reactor = { workspace = true, features = ["test"] }

[build-dependencies]
windows-reactor-setup = { workspace = true }

[lints]
workspace = true
