[package]
name = "test_reactor"
version = "0.0.0"
edition = "2024"
publish = false

[dev-dependencies]
rustc-hash = { workspace = true }
windows-core = { workspace = true }

[dependencies]
windows-reactor = { workspace = true, features = ["test-util"] }

[build-dependencies]
windows-reactor-setup = { workspace = true }

[lints]
workspace = true
