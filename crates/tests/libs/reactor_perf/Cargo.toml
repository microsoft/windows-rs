[package]
name = "test_reactor_perf"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
windows-reactor = { workspace = true }
windows = { workspace = true, features = ["Win32_System_ProcessStatus", "Win32_System_Threading"] }
windows-core = { workspace = true }

[build-dependencies]
windows-reactor-setup = { workspace = true }

[lints]
workspace = true
