/* Forward-declared class with UUID — emitted as a GUID constant. */
class __declspec(uuid("e6756135-1e65-4d17-8576-610761398c3c"))
DiaSource;

/* Forward-declared class without UUID — silently ignored. */
class DiaSomeOther;
