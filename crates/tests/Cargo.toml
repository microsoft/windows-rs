[package]
name = "tests"
version = "0.3.1"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../.." }
gen = { package = "windows_gen", path = "../gen",  version = "0.3.1" }

[dev-dependencies]
futures = "0.3"

[build-dependencies]
windows = { path = "../.." }
