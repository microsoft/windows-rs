[package]
name = "tests"
version = "0.1.3"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../.." }
windows_winmd = { path = "../winmd",  version = "0.1.3" }

[dev-dependencies]
futures = "0.3"

[build-dependencies]
windows = { path = "../.." }
