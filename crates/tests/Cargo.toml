[package]
name = "tests"
version = "0.2.1"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../.." }
windows_winmd = { path = "../winmd",  version = "0.2.1" }

[dev-dependencies]
futures = "0.3"

[build-dependencies]
windows = { path = "../.." }
