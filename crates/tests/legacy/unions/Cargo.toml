[package]
name = "test_unions"
version = "0.0.0"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../../../.." }
gen = { package = "windows_gen", path = "../../../../crates/deps/gen" }

[build-dependencies]
windows = { path = "../../../..", features = ["build"] }
