[package]
name = "test_pwstr"
version = "0.0.0"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../../../libs/windows", features = ["build", "alloc"] }

[build-dependencies]
windows = { path = "../../../libs/windows", features = ["build", "alloc"] }

[features]
default = ["alloc"]
alloc = []
std = []
