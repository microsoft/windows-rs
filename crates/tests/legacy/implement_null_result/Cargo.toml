[package]
name = "test_implement_null_result"
version = "0.0.0"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../../../libs/windows" }

[build-dependencies]
windows = { path = "../../../libs/windows", features = ["build"] }
