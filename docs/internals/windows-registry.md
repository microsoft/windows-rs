# windows-registry — internals

> Maintenance notes for `windows-registry`. For usage, see the [guide](../guide/windows-registry.md).

- 📁 [Source](https://github.com/microsoft/windows-rs/tree/master/crates/libs/registry)

## How it's built

`src/bindings.rs` is generated by `tool_bindings` from `crates/tools/bindings/src/registry.txt`; `Key`, `Value`, `OpenOptions`, and `Transaction` are hand-written safe wrappers.

## Testing

_See the workspace test crates and `cargo test -p windows-registry`._
