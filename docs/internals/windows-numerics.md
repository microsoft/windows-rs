# windows-numerics — internals

> Maintenance notes for `windows-numerics`. For usage, see the [guide](../guide/windows-numerics.md).

- 📁 [Source](https://github.com/microsoft/windows-rs/tree/master/crates/libs/numerics)

## How it's built

`src/bindings.rs` is generated by `tool_bindings` from `crates/tools/bindings/src/numerics.txt`; the inherent methods and operator trait impls are hand-written.

## Testing

_See the workspace test crates and `cargo test -p windows-numerics`._
