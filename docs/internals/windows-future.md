# windows-future — internals

> Maintenance notes for `windows-future`. For usage, see the [guide](../guide/windows-future.md).

- 📁 [Source](https://github.com/microsoft/windows-rs/tree/master/crates/libs/future)

## How it's built

`src/bindings.rs` is generated by `tool_bindings` from `crates/tools/bindings/src/future.txt` and `future_impl.txt`; the `Future` bridge is hand-written.

## Testing

_See the workspace test crates and `cargo test -p windows-future`._
