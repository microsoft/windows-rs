# windows-time — internals

> Maintenance notes for `windows-time`. For usage, see the [guide](../guide/windows-time.md).

- 📁 [Source](https://github.com/microsoft/windows-rs/tree/master/crates/libs/time)

## How it's built

`src/bindings.rs` is generated by `tool_bindings` from `crates/tools/bindings/src/time.txt`; the conversion logic is hand-written. See the guide for the design rationale.

## Testing

_See the workspace test crates and `cargo test -p windows-time`._
