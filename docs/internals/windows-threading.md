# windows-threading — internals

> Maintenance notes for `windows-threading`. For usage, see the [guide](../guide/windows-threading.md).

- 📁 [Source](https://github.com/microsoft/windows-rs/tree/master/crates/libs/threading)

## How it's built

`src/bindings.rs` is generated by `tool_bindings` from `crates/tools/bindings/src/threading.txt`; `Pool` and the `submit`/`for_each` helpers are hand-written.

## Testing

_See the workspace test crates and `cargo test -p windows-threading`._
