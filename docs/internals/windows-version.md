# windows-version — internals

> Maintenance notes for `windows-version`. For usage, see the [guide](../guide/windows-version.md).

- 📁 [Source](https://github.com/microsoft/windows-rs/tree/master/crates/libs/version)

## How it's built

`src/bindings.rs` is generated by `tool_bindings` from `crates/tools/bindings/src/version.txt`; `OsVersion` and the comparison logic are hand-written.

## Testing

_See the workspace test crates and `cargo test -p windows-version`._
