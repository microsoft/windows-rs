# windows-services — internals

> Maintenance notes for `windows-services`. For usage, see the [guide](../guide/windows-services.md).

- 📁 [Source](https://github.com/microsoft/windows-rs/tree/master/crates/libs/services)

## How it's built

`src/bindings.rs` is generated by `tool_bindings` from `crates/tools/bindings/src/services.txt`; the service runtime and dispatch glue are hand-written.

## Testing

_See the workspace test crates and `cargo test -p windows-services`._
