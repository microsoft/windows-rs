# Test consolidation plan: `windows-bindgen`, `windows-rdl`, `windows-metadata`

## 1. Background — current testing strategies

The repo currently has five overlapping testing strategies for the parser/generator stack:

| # | Location | What it does | Strengths | Drawbacks |
|---|---|---|---|---|
| A | `crates/tests/roundtrip/rdl` | Auto-scans `src/*.rdl`, reads RDL → winmd → RDL, asserts the file is byte-stable. ~60 cases, build.rs generates `#[test]` functions. | Concise, catches a huge class of reader/writer asymmetries, easy to add new cases. | Only proves *fixed-point*; symmetric bugs in both reader & writer slip through. Doesn't validate the *intermediate* winmd or any generated Rust. Hard-codes the `Test` namespace filter. |
| B | `crates/tests/roundtrip/clang` | Auto-scans `src/*.h`, runs Clang → RDL → winmd → RDL. ~35 cases. | Same. Also exercises clang frontend. | Same as (A); also `target_pointer_width = "64"` only. |
| C | `crates/tests/roundtrip/bindgen` | Auto-scans `src/*.rdl`; reads RDL → winmd → RDL (roundtrip) **and** runs `windows-bindgen` to commit `src/*.rs`. | Adds bindgen output validation in addition to the RDL roundtrip. | Generated `.rs` files are committed and reviewed via diff. Only 7 cases today; the same coverage idea isn't applied to the older `tests/libs/bindgen` golden snapshots. |
| D | `crates/tests/libs/rdl/tests/*.rs` (≈25 files) | Mix of: bespoke roundtrip (`use-declarations`, `directory`, `split`, `nested-arches`, `path`, `struct_fields`, `struct_values`, `mod-recursive*`, `default-interface`, `const-underlying*`, `guid-derive`, `fn_abi`, `nested-packing`, `exclusive-to`, `assembly_name`); error/panic harnesses (`panic.rs` ~50 inline RDL strings, `error.rs`, `writer_errors.rs`, `invalid_output.rs`); flag/feature checks (`opt.rs`, `filter.rs`). | Captures behaviour the roundtrip framework can't (errors, type-system invariants, file-IO modes, golden Rust output). | Each test reinvents temp-file plumbing, every "happy-path" file duplicates what roundtrip tests already do. ~half of these could be deleted by porting the `.rdl` file into `roundtrip/rdl/src/`. |
| E | `crates/tests/libs/bindgen/src/*.rs` (~110 files) generated by `crates/tools/bindgen` | Imperative tool runs `bindgen` with a one-liner per case against the prebuilt `default/*.winmd`, commits the output. A few `tests/*.rs` exercise the resulting bindings at runtime. | Concise definition (one line per fixture). Drives diff-based review. | The list of cases lives in tool source, not as data; cannot be filtered/parallelized; no equivalent for *RDL → bindgen* output (which is what most users actually drive). |
| F | `crates/tests/libs/metadata/tests/*.rs` | Direct `writer::File` API tests (`empty`, `struct`, `attribute`, `class`, `interface`), `reader` queries against `default/Windows*.winmd`, `merge.rs` (RDL→winmd, then `windows_metadata::merge()`, then RDL writer). | Tests the metadata APIs in isolation, the only place merge is exercised. | `merge.rs` could be reframed as a roundtrip case (RDL+RDL → merged winmd → golden RDL). Direct-writer tests are valuable but don't compose with the rest. |
| G | `crates/tests/misc/**`, `crates/tests/winrt/**` | Older runtime tests for compiled bindings against real `windows`/`windows-sys` crates. | Only place exercising actual Windows behaviour. | Out of scope for *generation* testing; many duplicate what bindgen-output snapshots can prove statically. Several are obsolete vs. the newer `tests/libs/bindgen` framework. |

## 2. Key drawbacks of the current roundtrip framework we must keep addressing

Roundtrip is convenient but has three real limitations that drive the existence of the other test styles:

1. **Fixed-point ≠ correctness.** A reader/writer pair that mis-encodes the same construct will still roundtrip. We need *cross-checks* (golden Rust from bindgen, structural assertions on the winmd, ECMA-335 `ildasm`-style dumps, runtime tests).
2. **No assertion on intermediate winmd.** Tests can't easily say "this RDL produces a `TypeRef` to `System.Enum` with these flags."
3. **Hard to express *negative* cases.** Errors, panics, conflicting attributes, invalid types — all live outside the roundtrip framework today.
4. **Limited input shapes.** Roundtrip uses one filter (`Test`) and one entry point (`reader().input(rdl)`); many code paths (CLI argv, multi-input directories, `--reference`, `--split`, `--no-deps`, `--flat`, `--sys`) need separate fixtures.

## 3. Goals

1. **One canonical "fixture-per-feature" model** — each language feature is described by a small directory of inputs and one or more golden outputs; tests are auto-generated from the directory, not enumerated in code.
2. **Layered assertions** — every fixture can opt into roundtrip, golden-winmd, golden-Rust, and (where applicable) compile/run checks, instead of forcing each test author to wire it up by hand.
3. **Centralize negative tests** as data, not as `should_panic` boilerplate.
4. **Eliminate duplication** between `tests/libs/rdl`, `tests/libs/metadata`, `tests/libs/bindgen`, and `tests/roundtrip/*`.
5. **Increase coverage** in the gaps revealed by the audit (error sites, merge, multi-arch, references, CLI, split mode, attribute encoding edge cases, large real-world winmds).

## 4. Proposed unified architecture

### 4.1 A single fixture format under `crates/tests/fixtures/`

Each fixture is one directory:

```
crates/tests/fixtures/<group>/<name>/
    input.rdl              (or input.h for clang fixtures, or input.toml for CLI)
    refs/                  (optional extra inputs / referenced winmds, by filename)
    expected.rdl           (writer-roundtrip golden)
    expected.winmd.txt     (optional: human-readable disassembly of the winmd)
    expected.rs            (optional: bindgen golden Rust)
    expected.err           (optional: expected reader/writer error message)
    fixture.toml           (declares which checks to run, filter, flags, --sys etc.)
```

`fixture.toml` is the single source of declarative knobs that today are hard-coded into per-test Rust files (filter name, `--sys`, `--no-deps`, `--flat`, `--reference`, `--split`, multi-arch bits, `target_pointer_width`, optional `expected_err`).

### 4.2 A single `test_fixtures` harness crate

A new crate (replacing today's three separate `roundtrip/*` crates) auto-discovers every fixture directory at build time and emits one `#[test]` per directory per enabled check:

- `roundtrip_<name>` – read inputs → write winmd → write back RDL → diff against `expected.rdl`
- `winmd_<name>` – dump `expected.winmd.txt` and diff (uses `windows_metadata::reader`)
- `bindgen_<name>` – run `windows_bindgen` with declared options and diff `expected.rs` (and `cargo build` it via a generated `lib.rs`)
- `error_<name>` – assert reader/writer fails with `expected.err` (replaces all `should_panic` boilerplate; far more diffable than embedded strings)
- `clang_<name>` – when an `input.h` is present, run Clang → RDL → diff
- `merge_<name>` – when multiple inputs are declared, run `windows_metadata::merge()` and diff
- `cli_<name>` – when `argv.txt` is present, drive `bindgen()`/`bindgen-cli` end-to-end

Update mode: `UPDATE_GOLDEN=1 cargo test` rewrites `expected.*` files from actual output, so authors add a fixture by dropping a directory and running tests once with the env var. (Same UX `tool_bindgen` provides today, but for *all* outputs uniformly.)

### 4.3 Replace `tool_bindgen` with the same harness

Today `crates/tools/bindgen` enumerates ~110 imperative `test()` calls. Convert each call to a fixture directory whose `fixture.toml` records the CLI args, and let the harness regenerate `expected.rs`. The tool becomes a 5-line wrapper that simply runs the harness in update mode. This collapses three regeneration mechanisms into one.

### 4.4 Extend the same model to clang fixtures

Replace `crates/tests/roundtrip/clang` with fixtures whose `input.h` triggers the `clang_*` check. This restores cross-platform skipping via `fixture.toml` (`requires_target_pointer_width = 64`) instead of a top-of-file `#![cfg]`.

### 4.5 Negative-case fixtures replace `panic.rs`

Each `should_panic` test in `crates/tests/libs/rdl/tests/panic.rs` (~50 cases) becomes a fixture with `input.rdl` and `expected.err` containing the canonical error message. Benefits: easier diff review, no escaped string literals, easy to grep, the same harness runs them.

### 4.6 Keep direct-API tests, but consolidate

`crates/tests/libs/metadata/tests/{empty,struct,attribute,class,interface,reader}.rs` exercise the `windows_metadata::writer::File` builder API directly. These don't fit the fixture model and should stay, but should move under `crates/tests/libs/metadata/tests/` as the *only* `tests/libs/metadata` content, with `merge.rs`, `merge_*.rdl`, `assembly_name.rs`, `load_library.rs` migrated into fixtures.

## 5. Concrete consolidation list (what to delete/rewrite)

A. **Migrate to fixtures (delete the bespoke `.rs` test):**
- `tests/libs/rdl/tests/struct_fields.rs`, `struct_values.rs`, `nested-arches.rs`, `nested-packing.rs`, `mod-recursive.rs`, `path.rdl/.rs`, `default-interface.rs`, `const-underlying*.rs`, `guid-derive.rs`, `fn_abi.rs`, `directory.rs`, `split.rs`, `use-declarations*.rs`, `exclusive-to.rs`, `opt.rs`, `filter.rs`, `assembly_name.rs`, `invalid_output.rs`.
- `tests/libs/metadata/tests/merge.rs` + all `merge_*.rdl` (use the `merge_*` check).
- All seven cases in `tests/roundtrip/bindgen/src/*.rdl` (now live under fixtures with `bindgen_*` enabled).
- All cases in `tests/roundtrip/rdl/src/*.rdl` and `tests/roundtrip/clang/src/*.{h,rdl}`.

B. **Convert to error-fixtures:**
- All ~50 `should_panic` tests in `tests/libs/rdl/tests/panic.rs`.
- The arch error tests in `tests/libs/rdl/tests/error.rs`.
- The two writer-error tests in `tests/libs/rdl/tests/writer_errors.rs`.

C. **Convert to bindgen-fixtures (replaces `tool_bindgen` enumeration):**
- All entries in `crates/tools/bindgen/src/main.rs`.
- The committed `crates/tests/libs/bindgen/src/*.rs` files become `expected.rs` golden files inside their fixtures.
- The runtime `tests/libs/bindgen/tests/{bool,deps,delegate_*,panic,ref_params}.rs` stay (they exercise generated bindings on Windows) but consume the fixtures via path.

D. **Keep as-is:**
- `tests/libs/metadata/tests/{empty,struct,attribute,class,interface,reader}.rs` (direct builder/reader-API unit tests).
- `tests/libs/rdl/tests/error.rs::error_display` (formatting unit test).
- All `crates/tests/misc/**` and `crates/tests/winrt/**` runtime suites — these test the published `windows` / `windows-sys` crates, not the parser/generator stack.

## 6. New coverage to add (gaps surfaced by the audit)

1. **Bindgen-from-RDL fixtures.** Today only roundtrip→bindgen exists for 7 RDL inputs and only bindgen-from-default-winmd exists for ~110 cases. Cross the matrix: every `tests/libs/bindgen` case should additionally have an RDL-source variant where feasible, exercising the full RDL→winmd→bindgen pipeline.
2. **Multi-arch fixtures.** Only `merge_arch_*` exercises `arch_input`; add fixtures combining x86+x64+Arm64, with nested types and conditional members, and golden `SupportedArchitecture` bitmasks.
3. **Reference-winmd tests.** Add fixtures that import a referenced winmd and assert that *no* `TypeDef` for the referenced type is emitted (only `TypeRef`). Today this is covered indirectly by `use-declarations-ref` only.
4. **CLI / argv fixtures.** Drive `windows_bindgen::bindgen` with full argv, including malformed flags (negative cases), unknown filters, missing `--in`, conflicting `--reference` values.
5. **Split-mode and directory-input fixtures** with multiple namespaces, asserting per-file output.
6. **Merger negative cases.** Conflicting type definitions across inputs, mismatched arches, missing `arch_input` bits — none currently tested.
7. **Attribute encoding edge cases.** Empty arg lists, named-only args, enum-typed args resolved across input boundaries (the existing `writer_errors_on_missing_enum_type` is the only such test).
8. **Determinism.** Re-run each bindgen fixture twice, diff the two outputs (catches HashMap-iteration bugs that have bitten the writer historically; today only `sort.rs` exercises ordering implicitly).
9. **`assembly_name` derivation** for file vs. directory output, with and without explicit `--name`.
10. **Roundtrip stability for `default/Windows*.winmd`.** Add a fixture that takes the prebuilt large winmds, writes them out as RDL, reads them back, and diffs the winmd bytes (catches regressions in real-world coverage rather than synthetic mini-cases).

## 7. Phased rollout

1. **Land the harness and fixture format** alongside the existing tests; demonstrate it on 3–5 fixtures from each category. ✅ *Done — see "Phase 1 status" below.*
2. **Migrate `tool_bindgen`** to the harness; delete `crates/tools/bindgen/src/main.rs` body.
3. **Migrate panic/error tests** (mechanical translation; can be scripted). ✅ *Done — all 54 `panic.rs` tests have moved to fixtures and `panic.rs` has been deleted; see "Phase 3 status" below.*
4. **Migrate bespoke `tests/libs/rdl/tests/*` files**, deleting each as its fixture lands. 🚧 *In progress — see "Phase 4 status" below.*
5. **Collapse `tests/roundtrip/{rdl,clang,bindgen}` crates** into the harness; delete them.
6. **Add the new coverage** from §6 once the harness is stable.
7. **Document** the fixture format in a `crates/tests/fixtures/README.md` so contributors add data, not Rust.

### Phase 1 status

- [x] **Harness crate landed** at `crates/tests/fixtures/harness/` (crate name `test_fixtures`).
  - `build.rs` walks `data/<group>/<name>/` and emits one `#[test]` per fixture (same codegen pattern as the existing `roundtrip/{rdl,clang,bindgen}/build.rs`, so per-fixture parallelism is preserved — Cargo runs each `#[test]` on its own thread by default).
  - Each test scratch-writes under a unique `$OUT_DIR/scratch/<group>/<name>/` directory, so concurrent fixtures never share filesystem state. This was a deliberate change from the existing `roundtrip` crates, which overwrite their own `src/*.rdl`/`*.winmd` files in place; that scheme is parallel-safe only because every fixture's basename is unique. Routing scratch through `$OUT_DIR` removes that constraint and keeps the tree clean.
  - `UPDATE_GOLDEN=1 cargo test -p test_fixtures` rewrites every `expected.*`, matching the `tool_bindgen` UX uniformly across all output kinds.
  - Measured runtime on the demo fixtures: 0.46s wall for 21 tests, vs. 0.43s for 63 tests in the legacy `test_roundtrip_rdl`. Per-fixture cost is comparable; phase 5 should not regress overall wall-time.
- [x] **Demo fixtures (5 each)** for `rdl`, `clang`, `bindgen`, `error`, plus 1 for `merge` (the existing suite only has two merge cases total). The `bindgen` and `error` flows had no equivalent in the legacy roundtrip crates and exercise new harness paths end-to-end.
- [x] **Fixture format documented** at `crates/tests/fixtures/harness/data/README.md`.

### Phase 1 design decisions / deltas from §4

- **Fixture root is `crates/tests/fixtures/harness/data/<group>/<name>/`, not `crates/tests/fixtures/<group>/<name>/`** as originally sketched. Reason: the workspace `members` glob is `crates/tests/*/*`, so a sibling fixtures-only directory would either need a global `exclude` entry per group or would fail the workspace check. Co-locating the data inside the harness crate keeps the workspace declaration unchanged and means the harness can refer to fixtures via `CARGO_MANIFEST_DIR` without any extra path plumbing. The README in §4 still applies; only the on-disk root moved.
- **`fixture.toml` is parsed by a tiny in-tree key/value parser** rather than pulling in a TOML crate. It only accepts a strict subset of TOML (`key = "string"`, `key = true|false`, `key = ["string", ...]`), so a fixture written today will keep parsing if we later swap in a real TOML dependency. Supported keys for phase 1: `filter`, `references`, `no_allow`, `no_comment`, `specific_deps`. Anything richer (`--sys`, `--no-deps`, `--flat`, `--split`, `argv.txt`, multi-arch bits, `requires_target_pointer_width`) is deferred until the corresponding migration phase needs it, to avoid speculatively designing knobs nobody consumes.
- **`error` fixtures use `Result::Err` rather than `catch_unwind`.** `windows_rdl::reader().write()` already returns `Result<(), Error>`, so the harness asserts on the returned error directly. The error rendering substitutes the basename for the absolute input path so `expected.err` is portable across machines (the existing `panic.rs` tests embed `.rdl` in their messages because they use `input_str`; once we migrate them to fixtures the basename will be `input.rdl`, which is still diff-friendly).
- **`merge` fixtures discover inputs by the `input-*.rdl` glob** rather than declaring them in `fixture.toml`. Lexical ordering is intuitive (`input-a.rdl`, `input-b.rdl`) and makes per-input arch tagging trivial to add later (`input-x86.rdl`, `input-x64.rdl` driving `arch_input` calls).
- **`winmd_<name>` (structural dump) and `cli_<name>` checks from §4.2 are not yet implemented.** They aren't blocking for phase 1's "demonstrate it on 3–5 fixtures from each category" gate; we'll add them once a migration phase needs them so we can co-design the dump format with a real consumer.
- **Phase 1 leaves the original tests in place.** The new harness runs alongside `roundtrip/{rdl,clang,bindgen}`, `libs/rdl/tests/panic.rs` and `libs/metadata/tests/merge.rs`. Phases 2–5 delete the originals as their fixtures land.

### Phase 3 status — `panic.rs` migration

- [x] **Migrated 48 of 49 reader-error `#[should_panic]` tests** from `crates/tests/libs/rdl/tests/panic.rs` to `error/<name>/` fixtures (5 reader-error fixtures already existed from phase 1, bringing the total to 53). Mechanical migration: extract the inline RDL string into `input.rdl`, run `UPDATE_GOLDEN=1 cargo test -p test_fixtures` to write `expected.err`, verify the generated golden contains the original `should_panic(expected = …)` substring (after substituting `.rdl` → `input.rdl`), then delete the old `#[should_panic]` block.
- [x] **`panic.rs` shrunk from 875 lines / 54 tests to 58 lines / 1 test** (`writer_errors_on_missing_enum_type`, see below). The unused `should_panic` helper fn was deleted along with its callers.
- [x] **`writer_errors_on_missing_enum_type` migrated** to `error/writer_missing_enum_type/` using the new writer-error layout (see design notes below). `panic.rs` is now deleted entirely; **all 54 tests have moved to fixtures**.

### Phase 3 design decisions / deltas from §4

- **Fixture directory names drop the `_errors` suffix.** Original tests use names like `unknown_attribute_errors`/`positional_after_named_errors`; the suffix is redundant once the test lives in the `error/` group, so fixtures are named `unknown_attribute`/`positional_after_named`. The remaining `_errors` cases keep their suffix only when stripping it would collide with another fixture (none today).
- **`should_panic` substring matching is good enough as a verification gate.** Rust's `#[should_panic(expected = …)]` already does substring matching, so the migration verifies that the generated `expected.err` *contains* the original `expected = …` literal (after `.rdl` → `input.rdl` substitution). The new `expected.err` is more precise (full message + final source location) than the original substring, which is a deliberate strengthening: future regressions in the unmatched suffix will now fail tests instead of slipping through.
- **The `should_panic` helper and the `should_panic` Rust attribute are unrelated.** The helper was a wrapper around `windows_rdl::reader().input_str(rdl).output(".winmd").write().unwrap()` whose name advertised intent rather than mechanism. The migration deleted it along with the last caller.
- **Writer-error fixtures use a `kind = "writer"` knob plus a `defs-*.rdl` multi-input layout.** Implemented option (b) from the previous phase 3 status: `error/<name>/` may contain one or more `defs-*.rdl` files (lexical order) alongside `input.rdl`. The harness compiles each `defs-*.rdl` to a scratch winmd, then compiles `input.rdl` with those winmds as additional reader inputs (both reader steps must succeed), then runs the *writer* on `input.rdl`'s winmd alone (no def winmds) and asserts it fails. This composes with the future "reference-winmd" coverage from §6.3, which will reuse the same `defs-*.rdl` discovery pattern for the `bindgen` group. Only one writer-error fixture exists today (`error/writer_missing_enum_type/`) but the layout is the canonical shape future writer-error tests should follow.
- **Writer errors render without a path/line prefix.** `windows_rdl::writer_err!` constructs an `Error` with `file_name=""`, `line=0`, `column=0`, so the `Display` impl emits `\nerror: <msg>` (no `--> path:line:col` suffix). The harness uses `format!("{err}")` directly for writer-error fixtures rather than going through the path-substitution helper used for reader errors.

### Phase 4 status — bespoke `tests/libs/rdl/tests/*` migration

Strategy: migrate in batches by *shape*, starting with files that fit the existing `rdl` group (RDL → winmd → RDL roundtrip). Files that need new harness features are deferred and listed below so a future phase 4 batch can co-design the knob with a real consumer.

**Batch 1 (this commit) — pure-roundtrip with no harness changes:**

- [x] `opt.rs::opt_before_out` → `rdl/opt_before_out`. Inline RDL string moved into `input.rdl`; the regenerated `expected.rdl` shows that the writer canonicalizes `#[opt] #[out]` to `#[out] #[opt]`. The original test only asserted `contains("#[opt]")`, so the golden RDL is a strictly stronger check (it now also catches drift in attribute ordering or surrounding output).
- [x] `use-declarations.rs::parse` → `rdl/use_declarations`. The existing `use-declarations.rdl` / `use-declarations-out.rdl` files map directly to `input.rdl` / `expected.rdl` with no edits.
- [x] `use-declarations.rs::parse_with_reference` → `rdl/use_declarations_ref`. Uses `references = ["../../../libs/bindgen/default/Windows.winmd"]` in `fixture.toml`. Note: the harness's `rdl` runner currently passes `references` to *both* the reader and the writer, while the original test passed the reference winmd to the reader only. The output is byte-identical (the writer's `filter("Test")` already excludes the referenced types from emission), so the existing golden file is reused unchanged.

**Deferred — need a new harness check or knob; tracked here so the next batch can group them by feature:**

| Test file | Why it doesn't fit the current `rdl` group | Harness work needed |
|---|---|---|
| `struct_fields.rs`, `struct_values.rs`, `fn_abi.rs` | Run `windows_bindgen` with `--sys` + `--no-comment`, write the `.rs` into `crates/tests/libs/rdl/src/` (committed, then `use`d from the test for runtime `size_of`/`align_of`/`Debug` asserts) | Extend the `bindgen` group (or add a new `bindgen_sys` group) to accept `sys = true`/`no_comment = true` knobs and to write `expected.rs` somewhere a downstream test crate can `mod` it. The runtime asserts themselves can stay in `tests/libs/rdl` as a thin "consume the generated bindings" crate (per §6 of this doc). |
| `nested-arches.rs`, `nested-packing.rs`, `default-interface.rs` | `winmd → RDL` only (no roundtrip): consume a prebuilt `default/*.winmd`, write a single committed `.rdl` for human review. | Add a `winmd_to_rdl` group (or generalize `rdl` to allow omitting `input.rdl` and using a referenced winmd as the source) plus a `filter` knob in `fixture.toml`. The committed `.rdl` becomes `expected.rdl`. |
| `mod-recursive.rs` | Single reader, then four writer invocations with different filters, each emitting a separate committed `.rdl` for diff review. | Either split into four fixtures (one per filter, each with its own `expected.rdl`) once the `winmd_to_rdl` shape exists, or add a `[[outputs]]`-style array to `fixture.toml`. The first option is mechanically simpler and fits the "one fixture, one assertion" principle. |
| `directory.rs`, `split.rs` | Reader/writer take a *directory* path, not a single file. | Add a `directory_input = true` / `split = true` knob, and have the harness compare directory trees instead of single files. Best paired with the `tool_bindgen` migration in phase 2 since CLI cases hit the same code path. |
| `assembly_name.rs` | No input file at all (asserts the reader's auto-derived assembly name from the output basename); also uses `windows_metadata::reader::File::read` directly. | Could be expressed as an `rdl` fixture with an empty `input.rdl` plus an `expected.winmd.txt` structural dump (the §4.2 `winmd_<name>` check that's still unimplemented). Until then, it can stay where it is. |
| `invalid_output.rs` | Single `should_panic` for `reader().output(".").write()` (no input RDL). | Reuse the `error/<name>/` layout but allow `input.rdl` to be omitted when `kind = "reader_no_input"`. Low priority — one test. |
| `filter.rs` | Three roundtrips that each `assert!(contents.contains("…"))` rather than diffing the whole RDL. | The full-file diff in the existing `rdl` group is strictly stronger; once the inline RDL becomes `input.rdl`, the three filters become three separate fixtures with their own `expected.rdl`. Pure mechanical translation, just three fixtures to author. Defer to batch 2. |
| `exclusive-to.rs`, `guid-derive.rs`, `const-underlying*.rs` | Read back the produced winmd via `windows_metadata::reader` and assert on attribute *values* (e.g. derived GUIDs, `ExclusiveTo` targets, `const` underlying types). Roundtripping the RDL doesn't prove the attribute was encoded correctly. | Add the §4.2 `winmd_<name>` check (a textual structural dump of the winmd) so these become diffable goldens. This is the same gap §6.7 ("attribute encoding edge cases") calls out, so the dump format should be co-designed with that work. |
| `error.rs::error_display` | Plain unit test for the `Error` `Display` impl — does not consume the fixture format. | Keep as-is per §5.D. |
| `writer_errors.rs` | Mix of "writer succeeds for X" smoke tests and "writer returns Err for bad output path" — neither asserts on output content. | The "succeeds" cases should turn into ordinary `rdl` fixtures (their input RDL exists inline today). The two `Err` cases want a `kind = "writer"` fixture where the failure point is an output path / split mode rather than a missing reference, which the current `kind = "writer"` shape doesn't model. Defer to a later batch alongside the §6.4 CLI fixtures. |

## 8. Outcome

- ~200 hand-rolled test files / 180 imperative `test()` calls collapse to a single harness + ~250 fixture directories.
- One way to add a test (drop a directory), one way to update goldens (`UPDATE_GOLDEN=1`), one way to read CI failures (file diffs, not panic strings).
- Coverage strictly increases: every fixture gets a winmd-structure check and a determinism check for free; gaps in §6 close; the older one-off `.rs` tests stop drifting from the roundtrip suite.
- Roundtrip semantics remain (so we keep their convenience) but are no longer the *only* signal — golden Rust + structural winmd + error-string fixtures jointly close the "symmetric reader/writer bug" hole.
