[package]
name = "test_win32_arrays"
version = "0.5.0"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../.." }
gen = { package = "windows_gen", path = "../../crates/gen",  version = "0.5.0" }

[build-dependencies]
windows = { path = "../.." }
