[package]
name = "test_win32"
version = "0.3.1"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../.." }
gen = { package = "windows_gen", path = "../../crates/gen",  version = "0.3.1" }

[build-dependencies]
windows = { path = "../.." }
