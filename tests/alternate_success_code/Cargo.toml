[package]
name = "test_alternate_success_code"
version = "0.0.0"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../.." }

[build-dependencies]
windows = { path = "../.." }
