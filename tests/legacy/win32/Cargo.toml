[package]
name = "test_win32"
version = "0.0.0"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../../.." }
gen = { package = "windows_gen", path = "../../../crates/gen" }

[dev-dependencies]
helpers = { package = "test_helpers", path = "../helpers" }

[build-dependencies]
windows = { path = "../../.." }
