[package]
name = "test_convertible"
version = "0.0.0"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../../.." }
gen = { package = "windows_gen", path = "../../../crates/gen" }

[build-dependencies]
windows = { path = "../../..", features = ["build"] }
