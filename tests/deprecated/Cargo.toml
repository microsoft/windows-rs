[package]
name = "test_deprecated"
version = "0.8.0"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../.." }

[build-dependencies]
windows = { path = "../.." }

[features]
default = ["deprecated"]
deprecated = []
