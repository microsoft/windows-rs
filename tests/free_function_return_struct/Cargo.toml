[package]
name = "test_free_function_return_struct"
version = "0.0.0"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../.." }

[build-dependencies]
windows = { path = "../.." }
