[package]
name = "test_wildcard"
version = "0.6.1"
authors = ["Microsoft"]
edition = "2018"

[dependencies]
windows = { path = "../.." }
gen = { package = "windows_gen", path = "../../crates/gen",  version = "0.6.1" }

[build-dependencies]
windows = { path = "../.." }
